Improved
- Added compact in-file search, binary file previews, editor/app font controls, draggable topbar actions, file drag preview polish, and native macOS window-control alignment polish.
- Improved project-window restore/routing, panel startup placement, browser-preview syncing, frontend timer cleanup, and indexer store recovery.

Fixed
- Fixed native macOS window-control framing in fullscreen.
- Reverted the experimental AI chat workspace redesign from the alpha release line.
- Kept Liquid Glass topbar spike work out of the alpha release line.

Changed
- Wails v3 runtime is pinned to v3.0.0-alpha.87 for this alpha.

Security
- macOS alpha artifacts remain unsigned/not notarized Developer ID builds; update manifests still require signed ZIP verification when enabled.
